# NeuralFSM: 基于时序图网络的自适应有限状态机多智能体系统
# NeuralFSM: Adaptive Finite State Machine Multi-Agent System via Temporal Graph Networks

**论文文档 - 第三部分：时序图网络运行机制与数学公式**

---

## 3. 时序图网络核心机制

### 3.1 TGN的设计动机

时序图网络（TGN）是NeuralFSM系统的核心学习引擎。选择TGN作为核心机制的原因在于，它能够同时处理图结构和时序信息，这正是多智能体系统所需要的。在多智能体系统中，智能体之间的交互关系形成图结构，而交互的时间顺序形成时序信息。TGN能够同时建模这两种信息，学习最优的协作模式。

TGN的核心思想是维护每个节点的动态嵌入，这个嵌入随着时间演化，捕捉节点的历史信息和当前状态。当节点之间发生交互时，TGN更新相关节点的嵌入，学习交互的模式。这种设计使得TGN能够学习长期依赖关系，捕捉智能体协作的复杂模式。

TGN在NeuralFSM中实现三大核心功能：FSM状态转移预测、监听路径预测和保护机制集成。**关键创新在于，TGN接收问题嵌入作为输入，实现任务自适应的预测。**状态转移预测根据当前状态、上下文特征和问题嵌入，预测从当前状态转移到下一状态的概率分布；监听路径预测根据状态和问题嵌入，预测状态输出应该被哪些智能体监听；保护机制集成计算节点的信任分数，用于消息权重衰减。

### 3.2 TGN的四个核心模块

TGN由四个核心模块组成：记忆模块、消息函数、消息聚合器和时间编码。这四个模块协同工作，实现了节点嵌入的动态更新。

#### 3.2.1 记忆模块 (Memory Module)

记忆模块使用GRU（Gated Recurrent Unit）存储每个节点的历史交互信息。GRU是一种循环神经网络，能够捕捉时序依赖关系。对于每个节点，记忆模块维护一个隐藏状态，这个状态随着交互的发生而更新。

**记忆更新公式**：

```
m_v(t) = GRU(m_v(t⁻), msg_v(t))
```

**GRU详细展开**：

```
重置门: r_t = σ(W_r · [m_v(t⁻), msg_v(t)] + b_r)
更新门: z_t = σ(W_z · [m_v(t⁻), msg_v(t)] + b_z)
候选记忆: m̃_v(t) = tanh(W_h · [r_t ⊙ m_v(t⁻), msg_v(t)] + b_h)
最终记忆: m_v(t) = (1 - z_t) ⊙ m_v(t⁻) + z_t ⊙ m̃_v(t)
```

其中：
- `m_v(t⁻) ∈ ℝᵐ`：上一时刻的记忆向量（m维）
- `msg_v(t) ∈ ℝᵐ`：当前时刻聚合的消息向量
- `r_t, z_t ∈ ℝᵐ`：重置门和更新门的激活值（0到1之间）
- `⊙`：逐元素乘法（Hadamard product）
- `σ`：Sigmoid激活函数
- `W_r, W_z, W_h ∈ ℝᵐˣ²ᵐ`：可学习的权重矩阵
- `b_r, b_z, b_h ∈ ℝᵐ`：可学习的偏置向量

**参数数量**：
- 重置门：`m × 2m + m = 2m² + m`
- 更新门：`m × 2m + m = 2m² + m`
- 候选记忆：`m × 2m + m = 2m² + m`
- **总计**：`6m² + 3m`（假设m=128，约98,304个参数）

#### 3.2.2 消息函数 (Message Function)

消息函数计算从节点u到节点v的消息，融合源节点、目标节点、边特征和时间编码：

**消息计算公式**：

```
msg(u → v, t) = MLP([h_u(t), h_v(t), e_{uv}, ϕ(Δt_{uv})])
```

**MLP详细展开**：

```
输入拼接: x = [h_u(t), h_v(t), e_{uv}, ϕ(Δt_{uv})] ∈ ℝᵈⁱⁿ
隐藏层: h = ReLU(W₁ · x + b₁) ∈ ℝᵈʰⁱᵈ
输出: msg(u → v) = W₂ · h + b₂ ∈ ℝᵐ
```

其中：
- `h_u(t) ∈ ℝᵈᵘ`：源节点u的特征向量（du维）
- `h_v(t) ∈ ℝᵈᵛ`：目标节点v的特征向量（dv维）
- `e_{uv} ∈ ℝᵈᵉ`：边(u,v)的特征向量（de维，如通信类型、权重）
- `ϕ(Δt_{uv}) ∈ ℝᵈᵗ`：时间间隔的编码（dt维）
- `Δt_{uv} = t - t_last(u,v)`：从上次交互到现在的时间差
- `d_in = du + dv + de + dt`：输入维度总和
- `W₁ ∈ ℝᵈʰⁱᵈˣᵈⁱⁿ, b₁ ∈ ℝᵈʰⁱᵈ`：第一层MLP参数
- `W₂ ∈ ℝᵐˣᵈʰⁱᵈ, b₂ ∈ ℝᵐ`：第二层MLP参数

**参数数量**：
- 第一层：`d_hid × d_in + d_hid`
- 第二层：`m × d_hid + m`
- **总计**：`d_hid × d_in + d_hid + m × d_hid + m`（假设d_in=512, d_hid=256, m=128，约131,328个参数）

#### 3.2.3 消息聚合器 (Message Aggregator)

消息聚合器聚合来自所有邻居的消息。系统支持三种聚合策略：

**策略1：平均聚合 (Mean Aggregation)**

```
msg_v(t) = (1/|N(v)|) · Σ_{u ∈ N(v)} msg(u → v, t)
```

**策略2：注意力聚合 (Attention Aggregation)**

```
注意力权重: α_{uv} = softmax({attention_score(u, v) | u ∈ N(v)})
加权聚合: msg_v(t) = Σ_{u ∈ N(v)} α_{uv} · msg(u → v, t)
```

**注意力分数计算**：

```
attention_score(u, v) = (W_q · h_v)ᵀ · (W_k · h_u) / √d_k
```

其中：
- `α_{uv} ∈ [0, 1]`：从u到v的注意力权重，`Σ_u α_{uv} = 1`
- `W_q, W_k ∈ ℝᵈᵏˣᵈ`：Query和Key的投影矩阵
- `d_k`：注意力维度（通常是特征维度的平方根）

**策略3：最大池化 (Max Pooling)**

```
msg_v(t) = max({msg(u → v, t) | u ∈ N(v)})
```

#### 3.2.4 时间编码 (Temporal Encoding)

时间编码使用位置编码（Positional Encoding）来表示时间信息：

**时间编码公式**：

```
ϕ(Δt) = [cos(ω₁·Δt), sin(ω₁·Δt), cos(ω₂·Δt), sin(ω₂·Δt), ..., cos(ωₖ·Δt), sin(ωₖ·Δt)]
```

**频率设置**：

```
ω_i = 1 / (10000^(2i/d_t))  for i = 0, 1, ..., k-1
```

其中：
- `Δt ∈ ℝ⁺`：时间间隔（秒、时间步等）
- `ωᵢ ∈ ℝ⁺`：第i个频率（从低频到高频）
- `k`：频率数量（决定编码维度 `d_t = 2k`）
- `d_t`：时间编码的总维度

#### 3.2.5 嵌入更新器 (Embedding Updater)

嵌入更新器将所有信息融合，生成最终的节点嵌入：

**嵌入更新公式**：

```
z_v(t) = σ(W_emb · [h_v || m_v(t) || ϕ(Δt)] + b_emb)
```

其中：
- `h_v ∈ ℝᵈᵛ`：节点v的原始特征（如智能体角色、状态描述）
- `m_v(t) ∈ ℝᵐ`：更新后的记忆向量（包含历史交互信息）
- `ϕ(Δt) ∈ ℝᵈᵗ`：时间编码（距离上次更新的时间）
- `[·, ·, ·]` 或 `||`：向量拼接操作
- `W_emb ∈ ℝᵈˣ⁽ᵈᵛ⁺ᵐ⁺ᵈᵗ⁾`：嵌入投影矩阵
- `b_emb ∈ ℝᵈ`：偏置向量
- `σ`：激活函数（通常用ReLU或GeLU）
- `z_v(t) ∈ ℝᵈ`：最终的d维节点嵌入

### 3.5 节点嵌入更新过程

节点嵌入的更新过程是一个多步骤的过程。首先，当节点之间发生交互时，消息函数计算交互的消息。消息函数接收源节点在上一时刻的嵌入、目标节点在上一时刻的嵌入、边特征和时间编码作为输入，通过MLP计算消息向量。消息向量编码了交互的所有相关信息。

然后，消息聚合器聚合邻居节点的消息。如果节点有多个邻居，消息聚合器将所有邻居的消息聚合为一个统一的表示。聚合策略的选择取决于具体应用场景。对于需要所有信息的情况，使用均值聚合；对于需要重点关注重要信息的情况，使用注意力聚合；对于只需要最关键信息的情况，使用最大池化。

接着，记忆模块更新节点的隐藏状态。记忆模块接收节点的当前隐藏状态和聚合后的消息，通过GRU更新隐藏状态。GRU的门控机制使得记忆模块能够选择性地保留历史信息和融入新信息，实现了长期依赖关系的建模。

最后，嵌入更新模块根据更新后的隐藏状态、聚合消息和节点特征，计算节点的新嵌入。新嵌入融合了历史信息、当前交互信息和节点特征，是节点状态的完整表示。

### 3.6 FSM状态转移预测

在NeuralFSM系统中，TGN的一个重要应用是预测FSM的状态转移。状态转移预测是系统的核心功能之一，它决定了问题求解的流程。

#### 3.6.1 状态转移概率计算（任务自适应）

给定当前状态 `sᵢ`、上下文特征 `c` 和问题嵌入 `q_emb`，TGN计算转移到每个可能状态的概率：

```
P(sⱼ | sᵢ, c, q_emb) = softmax(TGN_transition(sᵢ, c, q_emb))ⱼ
```

**详细计算过程**：

```
1. 获取状态嵌入:
   z_sᵢ = TGN(sᵢ, N(sᵢ), history)

2. 编码问题嵌入:
   q_encoded = QuestionEncoder(q_emb) ∈ ℝʰⁱᵈ
   其中 q_emb ∈ ℝ³⁸⁴ 是问题文本的Sentence Transformer嵌入

3. 拼接特征（状态 + 上下文 + 问题）:
   features = [z_sᵢ, c, q_encoded] ∈ ℝ^(d + d_context + hidden)

4. 通过MLP预测logits:
   logits = MLP_transition(features) ∈ ℝ|S|

5. Softmax归一化:
   P(· | sᵢ, c, q_emb) = softmax(logits)
```

**转移预测MLP参数**：
- 输入维度：`d + d_context + hidden`（增加了问题嵌入维度）
- 隐藏层维度：`d_hid`
- 输出维度：`|S|`（状态数量）
- **参数数量**：`d_hid × (d + d_context + hidden) + d_hid + |S| × d_hid + |S|`

**问题嵌入编码器参数**：
- 输入维度：`384`（Sentence Transformer默认维度）
- 隐藏层维度：`d_hid`
- 输出维度：`d_hid`
- **参数数量**：`d_hid × 384 + d_hid + d_hid × d_hid + d_hid`

#### 3.6.2 状态转移损失

状态转移损失使用交叉熵损失，鼓励TGN预测出成功的转移序列：

```
L_trans = CrossEntropy(P(· | sᵢ, c), sⱼ_true)
```

其中 `sⱼ_true` 是真实的下一状态（从成功的任务执行历史中提取）。

### 3.7 监听关系预测

监听关系预测是TGN的另一个重要应用。在多智能体系统中，一个状态的输出可能需要被其他智能体使用，监听关系预测决定了哪些智能体应该监听哪些状态的输出。

#### 3.7.1 监听权重计算（任务自适应）

给定状态 `sᵢ`、智能体 `aⱼ` 和问题嵌入 `q_emb`，TGN计算监听权重：

```
w(sᵢ, aⱼ, q_emb) = TGN_listener(sᵢ, aⱼ, q_emb)
```

**详细计算过程**：

```
1. 获取状态和智能体嵌入:
   z_sᵢ = TGN(sᵢ, N(sᵢ), history)
   z_aⱼ = TGN(aⱼ, N(aⱼ), history)

2. 编码问题嵌入:
   q_encoded = QuestionEncoder(q_emb) ∈ ℝʰⁱᵈ

3. 拼接特征（状态 + 问题）:
   state_question = [z_sᵢ, q_encoded] ∈ ℝ^(d + hidden)

4. 计算注意力查询和键:
   query = AttentionQuery(state_question) ∈ ℝʰⁱᵈ
   key = AttentionKey(z_aⱼ) ∈ ℝʰⁱᵈ

5. 计算注意力分数:
   attention_score = (query · key) / √d_k

6. Softmax归一化得到权重:
   w(sᵢ, aⱼ, q_emb) = softmax(attention_score)
```

**监听预测参数**：
- 状态特征维度：`d`
- 智能体特征维度：`d`
- 问题嵌入维度：`384`
- 隐藏层维度：`d_hid`
- 输出维度：`1`（标量权重）
- **参数数量**：包括问题编码器、注意力查询和键的MLP参数

**概率采样**：

系统使用概率采样选择监听关系：

```
sampled_listeners ~ Multinomial(w(sᵢ, ·, q_emb), num_samples)
```

采样得到的智能体索引用于从总FSM中匹配对应的智能体。

#### 3.7.2 监听路径损失

监听路径损失使用二元交叉熵损失，鼓励TGN学习有效的通信路径：

```
L_listener = BinaryCrossEntropy(w(sᵢ, aⱼ), y_true)
```

其中 `y_true ∈ {0, 1}` 是真实的监听关系（从任务执行历史中提取）。

### 3.8 TGN的训练过程

TGN的训练过程是一个端到端的过程。系统使用多目标损失函数同时优化状态转移预测和监听关系预测。

对于状态转移预测，系统使用交叉熵损失。损失函数比较预测的概率分布和真实的下一个状态。真实的下一个状态可以从任务执行历史中提取，也可以通过专家标注得到。交叉熵损失鼓励系统预测高概率给真实的下一个状态，低概率给其他状态。

对于监听关系预测，系统使用二元交叉熵损失。损失函数比较预测的监听权重和真实的监听关系。真实的监听关系可以从任务执行历史中提取，也可以通过分析任务需求得到。二元交叉熵损失鼓励系统预测高权重给应该建立监听关系的状态-智能体对，低权重给不应该建立监听关系的对。

系统通过反向传播算法更新TGN的参数。梯度从损失函数反向传播到各个模块，更新记忆模块、消息函数、消息聚合器和嵌入更新模块的参数。训练过程持续多个epoch，直到损失函数收敛或达到最大训练轮数。

在典型配置下，TGN的总参数数量约为543,977个。其中记忆模块（GRU）约98,304个参数，消息函数（MLP）约131,328个参数，嵌入更新器（MLP）约114,944个参数，状态转移预测（MLP）约66,560个参数，监听路径预测（MLP）约131,841个参数。这些参数通过端到端训练，学习最优的状态转移和通信拓扑。

---

**第三部分结束**

*下一部分将详细阐述保护机制的设计和实现。*


