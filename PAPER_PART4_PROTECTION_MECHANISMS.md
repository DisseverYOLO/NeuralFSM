# NeuralFSM: 基于时序图网络的自适应有限状态机多智能体系统
# NeuralFSM: Adaptive Finite State Machine Multi-Agent System via Temporal Graph Networks

**论文文档 - 第四部分：保护机制设计**

---

## 4. 保护机制设计

### 4.1 保护机制的设计动机

在多智能体系统中，可能存在恶意攻击或异常行为。例如，某些智能体可能发送错误信息，干扰其他智能体的工作；某些智能体可能表现出异常的行为模式，影响系统的整体性能。NeuralFSM系统设计了保护机制，通过异常检测和信任机制，防御这些威胁。

保护机制的设计遵循几个核心原则。首先，多层次防御的原则。系统从多个角度检测异常，包括频率异常、语义异常、行为异常等，确保能够识别各种类型的威胁。其次，自适应调整的原则。系统根据节点的历史行为，动态调整信任分数和消息权重，适应不同的情况。第三，可学习的原则。系统使用可学习的MLP计算消息权重，能够从数据中学习最优的防御策略。第四，轻量级的原则。保护机制的计算开销小，不影响系统的正常运行。

**核心创新：双重防御策略**

NeuralFSM的保护机制采用双重防御策略，既在训练阶段优化系统结构，又在运行时提供动态保护：

1. **训练时防御**：通过保护损失函数，让TGN学习减少异常智能体的连接。保护损失函数惩罚高风险连接（异常源 → 关键目标）的强消息，通过梯度反向传播，TGN的参数会调整，使得异常连接的消息强度降低。

2. **运行时防御**：通过消息衰减机制，动态降低来自异常智能体的消息影响。系统计算每条消息的信任度，低信任消息被衰减（混合更多原始特征），极低信任的连接可能被直接过滤。

这种双重防御策略实现了全面的鲁棒性提升，既优化了系统结构，又提供了动态保护。

### 4.2 中心性分析与保护优先级

中心性分析是保护机制的基础，它识别系统中的重要节点，为这些节点分配更高的保护优先级。

#### 4.2.1 节点保护优先级

节点保护优先级通过两种中心性指标的加权融合计算：

```
π(i) = w_BC · BC(i) + w_PR · PR(i)
```

其中：
- `BC(i) ∈ [0, 1]`：节点i的介数中心性（归一化）
- `PR(i) ∈ [0, 1]`：节点i的PageRank中心性（归一化）
- `w_BC, w_PR ∈ [0, 1]`：权重系数，满足 `w_BC + w_PR = 1`
- 默认权重：`w_BC = 0.6, w_PR = 0.4`

#### 4.2.2 介数中心性 (Betweenness Centrality)

介数中心性衡量节点作为"桥梁"的重要性：

**数学定义**：

```
BC(i) = Σ_{s≠i≠t} [σ_st(i) / σ_st]
```

其中：
- `i`：目标节点
- `s, t`：源节点和目标节点对
- `σ_st`：从s到t的最短路径总数
- `σ_st(i)`：从s到t经过节点i的最短路径数

**归一化**：

```
BC_normalized(i) = BC(i) / [(n-1)(n-2)/2]
```

其中 `n` 是节点总数，`(n-1)(n-2)/2` 是最大可能的介数中心性值。

#### 4.2.3 PageRank中心性

PageRank中心性衡量节点的全局重要性：

**数学定义**：

```
PR(i) = (1-α)/n + α · Σ_{j→i} [PR(j) / deg_out(j)]
```

其中：
- `PR(i) ∈ [0, 1]`：节点i的PageRank分数（归一化）
- `α ∈ [0, 1]`：阻尼系数（默认0.85）
- `n`：图中节点总数
- `j→i`：所有指向节点i的节点j
- `deg_out(j)`：节点j的出度
- `(1-α)/n`：随机跳转概率（防止陷入孤立节点）

**迭代计算**：

```
初始化: PR⁽⁰⁾(i) = 1/n  # 所有节点初始分数相等

迭代更新（Power Iteration）:
for k in range(max_iter):
    PR⁽ᵏ⁺¹⁾(i) = (1-α)/n + α · Σ_{j→i} [PR⁽ᵏ⁾(j) / deg_out(j)]
    
    收敛判断:
    if ||PR⁽ᵏ⁺¹⁾ - PR⁽ᵏ⁾|| < ε:
        break
```

### 4.3 异常检测机制

异常检测是保护机制的核心，它识别系统中的异常行为，为信任计算提供依据。

#### 4.3.1 频率异常检测

频率异常检测使用Z-score方法检测消息频率的异常波动：

**数学定义**：

```
α_freq(i, t) = sigmoid(Z_score(count_i))
```

其中Z-score：

```
Z_score(count_i) = |count_i - μ_count| / σ_count
```

**符号说明**：
- `count_i`：节点i的通信次数（滑动窗口内）
- `μ_count`：所有节点通信次数的均值
- `σ_count`：所有节点通信次数的标准差
- `Z_score`：标准化偏离度
- `sigmoid(x) = 1/(1+e^(-x))`：映射到[0,1]

#### 4.3.2 语义异常检测

语义异常检测使用余弦相似度检测消息内容的语义偏差：

**数学定义**：

```
α_semantic(i, t) = 1 - cos_similarity(z_i(t), μ_emb_i)
```

其中余弦相似度：

```
cos_similarity(a, b) = (a · b) / (||a|| · ||b||)
```

**符号说明**：
- `z_i(t) ∈ ℝᵈ`：节点i在时刻t的TGN嵌入（d=256维）
- `μ_emb_i ∈ ℝᵈ`：节点i的历史嵌入均值（滑动窗口）
- `a · b`：向量点积
- `||a||`：向量L2范数
- `cos_similarity ∈ [-1, 1]`：相似度，1表示完全相同
- `α_semantic ∈ [0, 2]`：偏离度，0表示无偏离

#### 4.3.3 异常分数融合

综合异常分数通过可学习的权重融合频率异常和语义异常：

**融合公式**：

```
α(i, t) = λ_freq · α_freq(i, t) + λ_semantic · α_semantic(i, t)
```

其中：
- `λ_freq, λ_semantic ∈ [0, 1]`：融合权重，满足 `λ_freq + λ_semantic = 1`
- 默认权重：`λ_freq = 0.3, λ_semantic = 0.7`（语义异常更重要）

#### 4.3.4 攻击类型与异常检测方法的对应关系

NeuralFSM系统需要防御多种类型的攻击，不同的攻击类型对应不同的异常检测方法：

**核心异常检测方法（始终启用）**：

1. **频率异常检测** → 主要检测**频率攻击**（Frequency Attack）
   - 攻击特征：恶意节点突然发送大量消息或长时间不发送消息，导致通信频率异常
   - 检测方法：使用Z-score方法检测消息频率的异常波动
   - 检测公式：`α_freq(i, t) = sigmoid(Z_score(count_i))`

2. **语义异常检测** → 主要检测**语义攻击**（Semantic Attack）
   - 攻击特征：恶意节点发送与历史消息语义完全不同的消息，导致语义偏差
   - 检测方法：使用余弦相似度检测消息内容的语义偏差
   - 检测公式：`α_semantic(i, t) = 1 - cos_similarity(z_i(t), μ_emb_i)`

**扩展异常检测方法（按需启用）**：

3. **拜占庭攻击检测** → 检测**拜占庭攻击**（Byzantine Attack）
   - 攻击特征：恶意节点发送包含恶意关键词的错误信息，干扰系统运行
   - 检测方法：通过关键词匹配检测恶意输出（如'INCORRECT', 'ERROR', 'CONFUSION', 'MISLEAD', 'BYZANTINE'等）
   - 检测公式：`α_byzantine(i, t) = min(keyword_count / threshold, 1.0)`
   - 当检测到明显拜占庭攻击时（`α_byzantine > 0.5`），添加惩罚项到综合异常分数

4. **Sybil攻击检测** → 检测**Sybil攻击**（Sybil Attack）
   - 攻击特征：攻击者创建多个伪造身份节点，这些节点的特征高度相似
   - 检测方法：通过特征相似度检测伪造节点，计算节点嵌入之间的余弦相似度
   - 检测公式：`α_sybil(i, j) = (similarity(i, j) - threshold) / (1.0 - threshold)` if `similarity(i, j) > threshold`
   - 当检测到Sybil节点对时，添加惩罚项到综合异常分数

5. **自私攻击检测** → 检测**自私攻击**（Selfish Attack）
   - 攻击特征：恶意节点拒绝协作，不向其他节点发送消息或只接收消息
   - 检测方法：通过通信图出边分析检测拒绝协作行为
   - 检测公式：`α_selfish(i, t) = 1 - (out_degree(i) / expected_out_degree)`
   - 当检测到自私行为时，添加惩罚项到综合异常分数

**综合异常分数（包含扩展检测）**：

当启用扩展异常检测时，综合异常分数计算为：

```
α_total(i, t) = λ_freq · α_freq(i, t) + λ_semantic · α_semantic(i, t) + Σ penalties
```

其中 `penalties` 包括：
- 拜占庭攻击惩罚：`0.5 × α_byzantine(i, t)` if `α_byzantine > 0.5`
- Sybil攻击惩罚：`0.3 × α_sybil(i, j)` if `α_sybil > 0`
- 自私攻击惩罚：`0.2 × α_selfish(i, t)` if `α_selfish > threshold`

**攻击类型总结**：

| 攻击类型 | 主要检测方法 | 检测特征 | 是否核心 |
|---------|------------|---------|---------|
| 频率攻击 | 频率异常检测 | 消息频率异常波动 | ✅ 核心 |
| 语义攻击 | 语义异常检测 | 消息语义偏差 | ✅ 核心 |
| 拜占庭攻击 | 拜占庭攻击检测 | 恶意关键词 | ⚠️ 扩展 |
| Sybil攻击 | Sybil攻击检测 | 特征高度相似 | ⚠️ 扩展 |
| 自私攻击 | 自私攻击检测 | 拒绝协作 | ⚠️ 扩展 |
| 混合攻击 | 所有检测方法 | 多种攻击组合 | ✅ 综合 |

**设计说明**：

频率异常检测和语义异常检测是核心检测方法，始终启用，能够检测最常见的频率攻击和语义攻击。扩展异常检测方法针对特定的攻击类型，按需启用，通过添加惩罚项增强异常检测的覆盖范围。这种设计既保证了核心防御能力，又提供了针对特定攻击类型的精确检测。

### 4.4 信任分数计算

信任分数是保护机制的核心指标，它综合了异常分数和保护优先级，反映了节点的可靠性。

#### 4.4.1 信任分数公式

信任分数结合异常分数和保护优先级：

**核心公式**：

```
trust(i, t) = (1 - α(i,t)) · (1 + π(i))
```

其中：
- `α(i,t) ∈ [0, 1]`：节点i在时刻t的异常分数
- `π(i) ∈ [0, 1]`：节点i的保护优先级
- `trust(i, t) ∈ [0, 2]`：信任分数范围

#### 4.4.2 公式设计原理

**设计思想**：

1. **行为因子** `(1 - α(i,t))`：异常分数越高，信任度越低
   - 当 `α(i,t) = 0`（完全正常）：正常性 = 1
   - 当 `α(i,t) = 1`（完全异常）：正常性 = 0
   - **单调递减**：异常↑ → 正常性↓

2. **结构因子** `(1 + π(i))`：重要节点的信任基线更高
   - 当 `π(i) = 0`（不重要）：因子 = 1（基准）
   - 当 `π(i) = 1`（最重要）：因子 = 2（双倍放大）
   - **线性放大**：优先级↑ → 放大系数↑

3. **乘积形式**：两个因子相互强化或削弱
   - 正常且重要：高信任
   - 异常但重要：中等信任（结构重要性部分抵消异常）
   - 正常但不重要：基准信任
   - 异常且不重要：低信任

### 4.5 消息权重计算

消息权重是保护机制的最终输出，它决定了消息在系统中的传播强度。系统使用可学习的MLP计算消息权重，而非固定公式，这使得系统能够从数据中学习最优的防御策略。

#### 4.5.1 可学习MLP消息权重计算器

系统使用可学习的MLP计算消息权重，这是推荐的方法：

**网络架构**：

```
输入层: [trust(u), π(v), edge_features] ∈ ℝ³
隐藏层1: h₁ = ReLU(W₁ · input + b₁) ∈ ℝ⁶⁴
隐藏层2: h₂ = ReLU(W₂ · h₁ + b₂) ∈ ℝ³²
输出层: weight(u,v) = sigmoid(W₃ · h₂ + b₃) ∈ [0,1]
```

**参数数量**：
- 第一层：`64 × 3 + 64 = 256`
- 第二层：`32 × 64 + 32 = 2,080`
- 第三层：`1 × 32 + 1 = 33`
- **总计**：`2,369个参数`

#### 4.5.2 消息权重公式

**MLP前向传播**：

```
h₁ = ReLU(W₁ · [trust(u), π(v), edge_features] + b₁)
h₂ = ReLU(W₂ · h₁ + b₂)
weight(u, v) = sigmoid(W₃ · h₂ + b₃)
```

其中：
- `trust(u) ∈ [0, 2]`：源节点u的信任分数
- `π(v) ∈ [0, 1]`：目标节点v的保护优先级
- `edge_features ∈ ℝ`：边特征（如通信类型、历史权重等）

#### 4.5.3 消息权重的作用

消息权重用于衰减低信任消息：

```
message_weighted(u → v) = message(u → v) × weight(u, v)
```

如果 `weight(u, v) < threshold`（例如0.1），消息可能被完全过滤。

### 4.6 保护损失与训练（训练时防御）

保护损失是保护机制的训练目标，它鼓励系统衰减低信任消息，保护重要节点。**这是双重防御策略的第一层：训练时防御。**

#### 4.6.1 保护损失公式

保护损失鼓励系统衰减低信任消息：

**保护损失定义**：

```
L_protect = Σ_{(u,v) ∈ E} [risk(u,v) × message_strength(u,v)]
```

其中：
- `risk(u,v) = 1 - trust(u)`：边(u,v)的风险分数
- `message_strength(u,v) = ||message(u → v)||²`：消息强度（L2范数的平方）
- `E`：所有边的集合

#### 4.6.2 保护损失的作用（训练时防御）

保护损失通过梯度反向传播，鼓励系统：
- **降低异常节点的信任分数**：异常检测模块学习识别异常行为
- **衰减低信任消息的权重**：消息权重MLP学习降低异常消息的权重
- **减少异常连接**：TGN学习减少异常智能体到关键目标的连接

**训练时防御的工作流程**：

1. 计算保护损失：`L_protect = Σ risk(u,v) × ||m||²`
2. 反向传播梯度：`∇_θ L_protect` 更新TGN和保护机制参数
3. TGN学习优化：减少异常连接的消息强度，降低异常节点的信任分数

保护损失与其他损失项（策略损失、转移损失、监听损失、成本损失）一起，构成总损失函数。总损失函数通过加权求和，平衡多个目标。保护损失的权重通常较小（例如0.1），因为保护是辅助目标，不应该过度影响主要任务。

### 4.7 保护机制集成（双重防御策略）

保护机制通过ProtectedTGN类集成到TGN中。保护机制采用双重防御策略，既在训练阶段优化系统结构，又在运行时提供动态保护。

#### 4.7.1 训练时防御（保护损失函数）

训练时，保护机制的工作流程包括：首先，计算节点保护优先级，通过介数中心性和PageRank的加权融合得到；然后，检测节点异常，通过频率异常和语义异常的加权融合得到综合异常分数；接着，计算信任分数，结合异常分数和保护优先级；然后，计算消息权重，使用可学习的MLP；最后，计算保护损失，通过梯度反向传播更新TGN参数，让TGN学习减少异常连接。

**训练时防御的效果**：
- TGN学习减少异常智能体到关键目标的连接
- 异常检测模块学习识别异常行为模式
- 消息权重MLP学习降低异常消息的权重

#### 4.7.2 运行时防御（消息衰减机制）

运行时，保护机制的工作流程包括：首先，计算节点保护优先级（可缓存，静态）；然后，检测节点异常（动态，基于历史）；接着，计算信任分数；然后，计算消息权重；最后，应用消息衰减，动态降低低信任消息的影响。

**运行时防御的两种策略**：

1. **连接过滤（激进策略）**：
   ```
   if weight(u, v) < threshold:
       filter_edge(u → v)  # 直接过滤低信任连接
   ```

2. **消息衰减（保守策略）**：
   ```
   protected_output = trust × TGN_output + (1 - trust) × original_features
   ```
   低信任时，更多保留原始特征，减少异常消息的影响。

**运行时防御的效果**：
- 动态检测异常行为，实时调整消息权重
- 衰减低信任消息，减少异常影响
- 过滤极低信任连接，防止恶意传播

#### 4.7.3 ProtectedTGN对FSM-TGN的支持

ProtectedTGN完全支持FSM-TGN的参数，包括：
- `current_state_id`：当前状态ID
- `context_features`：任务上下文特征
- `question_embedding`：问题嵌入（任务自适应）
- `transition_history`：状态转移历史

ProtectedTGN自动检测FSM-TGN（通过检查`transition_predictor`和`listener_predictor`属性），并正确传递所有参数，返回包含`transition_probs`和`listener_weights`的字典格式输出。

保护机制的可学习参数包括：中心性融合权重（2个参数）、异常检测融合权重（2个参数）和消息权重MLP（2,369个参数），总计约2,373个可学习参数。这些参数通过端到端训练，学习最优的防御策略。

### 4.8 总损失函数与梯度

保护机制的保护损失与其他损失项一起构成系统的总损失函数。总损失函数通过加权求和，平衡多个目标。

#### 4.8.1 总损失函数

**总损失函数公式**：

```
L_total = α · L_policy + β · L_trans + γ · L_listener + δ · L_cost + λ · L_protect
```

其中：
- `L_policy`：策略梯度损失（任务导向，稀疏奖励）
- `L_trans`：状态转移损失（密集监督，细粒度反馈）
- `L_listener`：监听路径损失（通信优化，拓扑学习）
- `L_cost`：LLM成本损失（成本控制，经济性优化）
- `L_protect`：保护损失（鲁棒性，防御机制）

**默认权重**：
- `α = 1.0`（策略损失）
- `β = 0.3`（转移损失）
- `γ = 0.2`（监听损失）
- `δ = 0.1`（成本损失）
- `λ = 0.1`（保护损失，如果启用）

#### 4.8.2 总损失函数的梯度

总损失函数的梯度通过反向传播算法计算：

**总损失梯度**：

```
∇_θ L_total = α · ∇_θ L_policy + β · ∇_θ L_trans + γ · ∇_θ L_listener + δ · ∇_θ L_cost + λ · ∇_θ L_protect
```

其中 `θ` 表示所有可学习参数，包括：
- TGN的参数（记忆模块、消息函数、消息聚合器、嵌入更新器）
- 保护机制的参数（中心性融合权重、异常检测融合权重、消息权重MLP）

**梯度流向**：

1. **策略梯度** → TGN状态转移预测模块
2. **转移损失梯度** → TGN状态转移预测模块
3. **监听损失梯度** → TGN监听路径预测模块
4. **成本损失梯度** → 整个系统（通过减少LLM调用）
5. **保护损失梯度** → 保护机制各模块（异常检测、信任计算、消息权重MLP）

**保护损失的梯度（训练时防御）**：

保护损失的梯度反向传播到保护机制的各个模块：

```
∇_θ L_protect = Σ_{(u,v) ∈ E} [∇_θ risk(u,v) × message_strength(u,v) + risk(u,v) × ∇_θ message_strength(u,v)]
```

其中：
- `∇_θ risk(u,v) = -∇_θ trust(u)`：风险分数的梯度
- `∇_θ trust(u)`：信任分数的梯度，反向传播到异常检测和中心性分析模块
- `∇_θ message_strength(u,v)`：消息强度的梯度，反向传播到消息权重MLP和TGN

**关键作用**：保护损失的梯度让TGN学习减少异常连接，这是训练时防御的核心机制。通过梯度反向传播，TGN的参数会调整，使得异常连接的消息强度降低，实现了结构层面的防御优化。

**梯度裁剪**：

为了防止梯度爆炸，系统使用梯度裁剪：

```
if ||∇_θ L_total|| > threshold:
    ∇_θ L_total = ∇_θ L_total × (threshold / ||∇_θ L_total||)
```

其中 `threshold` 是梯度裁剪阈值（默认1.0）。

---

**第四部分结束**

*下一部分将详细阐述所有损失函数的设计和数学公式。*


