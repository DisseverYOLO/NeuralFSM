# NeuralFSM: 基于时序图网络的自适应有限状态机多智能体系统
# NeuralFSM: Adaptive Finite State Machine Multi-Agent System via Temporal Graph Networks

**论文文档 - 第五部分：损失函数设计**

---

## 5. 多目标损失函数设计

### 5.1 损失函数概述

NeuralFSM系统使用多目标损失函数同时优化多个目标，实现整体性能的协同优化。损失函数的设计遵循几个核心原则：目标平衡、权重可调、梯度稳定、效果可解释。

**总损失函数**：

```
L_total = α · L_policy + β · L_trans + γ · L_listener + δ · L_cost + λ · L_protect
```

其中：
- `L_policy`：策略梯度损失（任务导向，稀疏奖励，**任务自适应**）
- `L_trans`：状态转移损失（密集监督，细粒度反馈，**基于采样，任务自适应**）
- `L_listener`：监听路径损失（通信优化，拓扑学习，**基于采样，任务自适应**）
- `L_cost`：LLM成本损失（成本控制，经济性优化）
- `L_protect`：保护损失（鲁棒性，防御机制）

**关键变化（任务自适应）**：
- 所有损失函数现在都基于**问题嵌入 `q_emb`** 的TGN预测
- 状态转移损失和监听路径损失基于**实际采样的状态和监听关系**，而非预定义的真实值
- 这种设计使得系统能够学习任务自适应的FSM结构和通信路径

**默认权重**：
- `α = 1.0`（策略损失）
- `β = 0.3`（转移损失）
- `γ = 0.2`（监听损失）
- `δ = 0.1`（成本损失）
- `λ = 0.1`（保护损失，如果启用）

### 5.2 策略梯度损失 (Policy Gradient Loss)

#### 5.2.1 损失函数定义（任务自适应）

策略梯度损失是任务导向的稀疏奖励损失，使用REINFORCE算法，**现在包含问题嵌入**：

**损失函数公式**：

```
L_policy = -E[log P(τ | θ, q_emb) · R(τ)]
```

其中：
- `τ = (s₀, a₀, s₁, a₁, ..., sₜ, aₜ)`：状态-动作序列
- `P(τ | θ, q_emb)`：在参数θ和问题嵌入`q_emb`下生成序列τ的概率（**任务自适应**）
- `R(τ) ∈ {0, 1}`：任务奖励（成功=1.0，失败=0.0）
- `q_emb ∈ ℝ³⁸⁴`：问题文本的Sentence Transformer嵌入
- **关键变化**：序列概率现在依赖于问题嵌入，使得策略梯度能够学习任务自适应的转移序列

#### 5.2.2 序列概率计算（任务自适应）

序列概率是各个转移概率的乘积，**现在包含问题嵌入**：

```
P(τ | θ, q_emb) = Π_{t=0}^{T-1} P(s_{t+1} | s_t, c_t, q_emb, θ)
```

其中：
- `P(s_{t+1} | s_t, c_t, q_emb, θ)` 是TGN预测的状态转移概率（基于问题嵌入 `q_emb`）
- `q_emb ∈ ℝ³⁸⁴` 是问题文本的Sentence Transformer嵌入
- **关键变化**：转移概率现在依赖于问题嵌入，实现任务自适应的序列生成

#### 5.2.3 对数概率计算（任务自适应）

在实际实现中，使用对数概率避免数值下溢：

```
log P(τ | θ, q_emb) = Σ_{t=0}^{T-1} log P(s_{t+1} | s_t, c_t, q_emb, θ)
```

**关键变化**：对数概率计算现在包含问题嵌入，使得策略梯度能够学习任务自适应的转移序列。

#### 5.2.4 损失函数展开（任务自适应）

**完整展开**：

```
L_policy = -Σ_{t=0}^{T-1} log P(s_{t+1} | s_t, c_t, q_emb, θ) · R(τ)
```

**批量版本**：

```
L_policy = -(1/B) · Σ_{i=1}^{B} [log P(τ_i | θ, q_emb_i) · R(τ_i)]
```

其中：
- `B` 是批次大小
- `q_emb_i` 是第i个问题的问题嵌入
- **关键变化**：每个问题的序列概率计算都使用其对应的问题嵌入，实现任务自适应

#### 5.2.5 梯度计算

策略梯度损失的梯度：

```
∇_θ L_policy = -E[∇_θ log P(τ | θ) · R(τ)]
```

**梯度方向**：
- 如果 `R(τ) = 1`（任务成功）：梯度方向是增加成功路径的概率
- 如果 `R(τ) = 0`（任务失败）：梯度方向是减少失败路径的概率

### 5.3 状态转移损失 (Transition Loss)

#### 5.3.1 损失函数定义（任务自适应，基于采样）

状态转移损失是密集的细粒度监督损失，**使用负对数似然（NLL）损失**，基于**实际采样的状态转移**：

**损失函数公式**：

```
L_trans = -Σ_{t} log P(s_{t+1}^sampled | s_t, c_t, q_emb, θ)
```

其中：
- `P(· | s_t, c_t, q_emb, θ)`：TGN预测的状态转移概率分布（**基于问题嵌入 `q_emb`**）
- `s_{t+1}^sampled`：**实际采样的下一状态**（从`transition_history`中提取，通过概率采样得到）
- **关键变化**：
  1. 损失基于**采样后的状态转移**，而非预定义的真实转移
  2. 概率分布现在包含问题嵌入，实现任务自适应
  3. 使用**负对数似然（NLL）**而非交叉熵，因为只关注采样到的状态

#### 5.3.2 负对数似然展开

**详细展开**：

```
L_trans = -(1/|T|) · Σ_{t ∈ T} log P(s_{t+1}^sampled | s_t, c_t, q_emb, θ)
```

其中：
- `T`：所有状态转移步骤的集合
- `s_{t+1}^sampled`：在步骤t实际采样得到的下一状态
- **计算方式**：对于每个采样的状态转移，计算其概率的负对数，然后取平均

#### 5.3.3 批量版本

**批量负对数似然损失**：

```
L_trans = -(1/B) · Σ_{i=1}^{B} (1/|T_i|) · Σ_{t=0}^{T_i-1} [log P(s_{t+1}^{i,sampled} | s_t^i, c_t^i, q_emb_i, θ)]
```

其中：
- `B`：批次大小
- `T_i`：第i个序列的长度
- `q_emb_i`：第i个问题的问题嵌入
- `s_{t+1}^{i,sampled}`：第i个序列在步骤t实际采样得到的下一状态

**实现细节**：
- 从`fsm_manager.transition_history`中获取所有实际执行的状态转移
- 对每个转移计算负对数似然：`-log(P(target_state))`
- 对所有转移的损失取平均

### 5.4 监听路径损失 (Listener Loss)

#### 5.4.1 损失函数定义（任务自适应，基于采样）

监听路径损失是通信拓扑优化的损失，使用二元交叉熵损失，基于**实际采样的监听关系**：

**损失函数公式**：

```
L_listener = BinaryCrossEntropy(w(s, a, q_emb), y_sampled(s, a))
```

其中：
- `w(s, a, q_emb) ∈ [0, 1]`：TGN预测的状态s向智能体a发送消息的权重（**基于问题嵌入 `q_emb`**）
- `y_sampled(s, a) ∈ {0, 1}`：**实际采样的监听关系**（1表示实际采样到该监听关系，0表示未采样）
- **关键变化**：
  1. 权重计算现在包含问题嵌入，实现任务自适应
  2. 目标值基于**采样后的监听关系**，而非预定义的真实关系
  3. 采样关系从`fsm_manager.listeners`中提取（通过概率采样得到）

#### 5.4.2 二元交叉熵展开

**详细展开**：

```
L_listener = -Σ_{s ∈ S} Σ_{a ∈ A} [y_sampled(s,a) · log w(s,a,q_emb) + (1-y_sampled(s,a)) · log(1-w(s,a,q_emb))]
```

其中：
- `y_sampled(s,a) = 1` 如果状态s实际采样到智能体a作为监听者，否则 `y_sampled(s,a) = 0`
- `w(s,a,q_emb)` 是TGN基于问题嵌入预测的监听权重

#### 5.4.3 批量版本

**批量二元交叉熵损失**：

```
L_listener = -(1/B) · Σ_{i=1}^{B} Σ_{s ∈ S} Σ_{a ∈ A} [y_{sampled}^i(s,a) · log w^i(s,a,q_emb_i) + (1-y_{sampled}^i(s,a)) · log(1-w^i(s,a,q_emb_i))]
```

其中：
- `B`：批次大小
- `q_emb_i`：第i个问题的问题嵌入
- `y_{sampled}^i(s,a)`：第i个问题中状态s实际采样到的监听关系
- `w^i(s,a,q_emb_i)`：第i个问题中TGN基于问题嵌入预测的监听权重

**实现细节**：
- 从`fsm_manager.listeners`中获取所有实际执行的监听关系
- 构建目标矩阵：`targets[state_id, agent_idx] = 1.0` 如果该监听关系被采样
- 使用二元交叉熵比较预测权重和目标矩阵

### 5.5 LLM成本损失 (Cost Loss)

#### 5.5.1 损失函数定义

LLM成本损失是LLM使用成本的惩罚项，鼓励系统减少LLM调用成本：

**损失函数公式**：

```
L_cost = (C_episode - C_baseline)²
```

其中：
- `C_episode`：当前episode的LLM调用成本
- `C_baseline`：基线成本（可以是历史平均成本或目标成本）

#### 5.5.2 成本计算

**episode成本**：

```
C_episode = Σ_{call ∈ episode} [tokens_input(call) × price_input + tokens_output(call) × price_output]
```

其中：
- `tokens_input(call)`：输入token数量
- `tokens_output(call)`：输出token数量
- `price_input`：输入token价格（美元/1000 tokens）
- `price_output`：输出token价格（美元/1000 tokens）

#### 5.5.3 基线成本估计

**基线成本估计**：

```
C_baseline = (1/N) · Σ_{i=1}^{N} C_episode_i
```

其中 `N` 是历史episode数量。

#### 5.5.4 批量版本

**批量成本损失**：

```
L_cost = (1/B) · Σ_{i=1}^{B} [(C_episode_i - C_baseline)²]
```

### 5.6 保护损失 (Protection Loss)

#### 5.6.1 损失函数定义

保护损失是保护机制的损失项，鼓励系统衰减低信任消息：

**损失函数公式**：

```
L_protect = Σ_{(u,v) ∈ E} [risk(u,v) × message_strength(u,v)]
```

其中：
- `risk(u,v) = 1 - trust(u)`：边(u,v)的风险分数
- `message_strength(u,v) = ||message(u → v)||²`：消息强度（L2范数的平方）
- `E`：所有边的集合

#### 5.6.2 批量版本

**批量保护损失**：

```
L_protect = (1/B) · Σ_{i=1}^{B} Σ_{(u,v) ∈ E_i} [risk_i(u,v) × message_strength_i(u,v)]
```

### 5.3 损失函数的组合与优化

总损失函数通过加权求和，平衡多个目标。权重设计的原则是：策略损失是主要目标，权重最大（α=1.0），确保任务准确率；辅助损失权重较小，避免过度干扰主要目标；权重可调，根据应用场景调整权重平衡。

系统通过反向传播算法更新模型参数。梯度从损失函数反向传播到各个模块，更新TGN的参数和保护机制的参数。为了防止梯度爆炸，系统使用梯度裁剪。当梯度的范数超过阈值时，系统将梯度缩放，使其范数等于阈值。这防止了梯度爆炸，提升了训练稳定性。

系统使用Adam优化器进行训练。Adam优化器结合了动量和自适应学习率的优点，能够稳定高效地训练深度神经网络。学习率可以配置，默认是0.001。系统支持学习率衰减，可以在训练过程中逐渐降低学习率，提升训练稳定性。


---

**第五部分结束**

*下一部分将详细阐述实验设计和结果。*


